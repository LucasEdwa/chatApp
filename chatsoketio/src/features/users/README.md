# Users Feature

User management, online status, and user list display.

## Structure

- **application/** - Use cases and hooks (`useUserFiltering`, `useMobileOptimization`)
- **domain/** - Interfaces and types (`IUser`)
- **infrastructure/** - (Reserved for future user-related services)
- **presentation/** - UI components (`UsersList`, `UserNameModal`, sub-components)
